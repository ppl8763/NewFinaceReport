import React from "react";

const Loading = () => {
    return (
        <div style={{ textAlign: "center", padding: "50px", fontSize: "18px" }}>
            <h2>⏳ Loading...</h2>
        </div>
    );
};

export default Loading;
